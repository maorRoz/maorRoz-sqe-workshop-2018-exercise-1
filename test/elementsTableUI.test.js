import {clean, createTable} from '../src/elementsTableUI';

describe('Element Table UI Tests' , () => {
    it('wtf1', () => {
        clean();
    });

    it('wtf2', () => {
        createTable([]);
    });
});